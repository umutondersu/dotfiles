# Use preset 1: FPS only
preset=1

# Make background fully transparent
background_alpha=0.0
alpha=1.0

# Gray text color (medium gray)
text_color=C0C0C0

# Optional: small, subtle font
font_size=18
position=top-left   # or top-right, bottom-left, bottom-right
hud_compact=yes     # makes it smaller and less obtrusive
