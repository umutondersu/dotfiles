# Sync to mounted drive at /media/qorcialwolf/T7
sync_dir = "~/OneDrive"

# Safety guards (prevent deletions if mount fails)
check_nomount = "true"
check_nosync  = "true"
classify_as_big_delete = "100"
