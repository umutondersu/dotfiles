context.properties = {
  default.clock.rate          = 96000                # Use desired sample rate
  default.clock.allowed-rates = [ 96000 48000 44100 ]
  default.clock.quantum       = 2048                 # 2048 samples @ 96kHz ≈ 21ms
  default.clock.min-quantum   = 1024                 # never go below 10ms
  default.clock.max-quantum   = 8192                 # up to ~85ms of buffering
  link.max-buffers            = 32                   # allows internal queueing (larger buffer pool)
}
