# Comments and blank lines are allowed.
\.git
\.gitignore

# Ignore everything at root that doesn't start with a dot
^/[^.][^/]*$

# Flatpaks
^\.var/app

# anacron runtime dirs (created at runtime, not stowed)
^\.anacron/logs
^\.anacron/spool
